// A Sample config settings. Create a copy with your settings and
// save it as config.js
module.exports = {
  app: {
    port: 8080,
    host: "127.0.0.1"
  },
  // development, production, maintenance
  env: 'development'
}